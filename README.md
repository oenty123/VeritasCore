# VeritasCore
Contract-oriented Python SAST with near-zero false positives. Zero dependencies, stdlib only. Learns guard contracts from your code, enforces consistency. Four-state analysis (guarded/unguarded/unknown/safe), auto-fix, cross-file taint, web dashboard, knowledge base. Principle Zero: never assert more than can be proven.
